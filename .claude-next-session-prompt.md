# BISINDO Bridge — Next Session Prompt

Copy-paste ini ke Claude Code di session baru:

---

```
PROJEK: bisindo-bridge — Two-way BISINDO (Bahasa Isyarat Indonesia) translator.
REPO: github.com/ItsKevinRafaell/bisindo-bridge (main branch, pushed & deployed)

STATUS SAAT INI (DONE):
- Capture page toggle 1/2 tangan (user pilih sesuai huruf BISINDO)
- Flask serve capture page (/capture) langsung dari laptop — same-origin, tanpa CORS/ngrok interstitial
- /api/health return counts per-letter (progress sync real-time)
- /api/sample accept 1 atau 2 tangan, num_hands akurat di CSV
- Classifier load fix (_DictLabelEncoder mock untuk legacy label dict)
- MediaPipe GPU→CPU auto-fallback
- Flask server auto-start via systemd (bisindo-server.service)
- Ngrok tunnel manual via start-ngrok.sh
- Classifier loaded: True (11,099 samples dari kaggle + legacy)
- dataset/landmarks_captured_v2.csv: 67-col, num_hands field (1 atau 2)

ARCHITECTURE:
- Vercel (bisindo-bridge.vercel.app): static hosting — landing, test page
- Flask (meeting/app.py, port 5000): serve capture page, REST API, meeting rooms
- Ngrok (start-ngrok.sh): expose laptop ke internet
- Teman buka: https://<ngrok-url>/capture (BUKAN vercel — supaya same-origin)
- Dataset: dataset/landmarks_captured_v2.csv (11,257 rows, di-push ke repo)
- Auto-push: setiap 100 sample baru → otomatis commit + push ke GitHub (via _try_auto_push di Flask)
- Target: 5,000 samples per letter

YANG BELUM KESELESAIAN (TODO):
1. Train model → butuh python 3.11/3.12 (tensorflow gak support 3.14).
   Cara: conda create -n bisindo python=3.11 → pip install tensorflow tensorflowjs → python train_landmark_model.py
   Output: web/models/model.json + labels.json + scaler.json
   Ini bikin /test.html jalan (browser TF.js inference)
2. Verify data collection → teman belum berhasil capture (CSV masih 11,099 rows, belum ada data baru dari capture)
3. Test page (/test.html) belum jalan karena model.json belum ada
4. Data collection: target 5,000 samples/letter — auto-push setiap 100 sample baru (Flask _try_auto_push)

CARA START SETIAP PAGI:
1. ./start-ngrok.sh → dapat URL ngrok
2. Share: <ngrok-url>/capture ke teman
3. Cek data: wc -l dataset/landmarks_captured_v2.csv

SERVICE COMMANDS:
- Flask: systemctl --user status/start/restart bisindo-server
- Ngrok: ./start-ngrok.sh (manual)
- Cek CSV: tail -1 dataset/landmarks_captured_v2.csv

NEXT ACTION YANG GW MAU:
- [ISI DI SINI: mau lanjut apa? train model, verify capture, dll]
```

---

## Quick Reference

### Endpoints
| URL | Description |
|-----|-------------|
| `https://<ngrok>/capture` | Data collection (dari Flask, same-origin) |
| `https://<ngrok>/api/health` | Health check + per-letter counts |
| `https://<ngrok>/api/sample` | POST landmark data → CSV |

### Files
- `meeting/app.py` — Flask server (port 5000)
- `meeting/templates/capture.html` — Template served by Flask (same-origin capture)
- `web/capture.html` — Vercel static version (pakai config.js)
- `dataset/landmarks_captured_v2.csv` — Dataset (git submodule, 11,099 rows)
