# BISINDO Bridge — Next Session Prompt

Copy-paste ini ke Claude Code di session baru:

---

```
PROJEK: bisindo-bridge — Two-way BISINDO (Bahasa Isyarat Indonesia) translator.
REPO: github.com/ItsKevinRafaell/bisindo-bridge (main branch, pushed & deployed)

STATUS SAAT INI (DONE):
- CSV schema: 67-col, 2-hand only, contributor field → dataset/landmarks_captured_v2.csv
- meeting/app.py: REST API (/api/sample, /api/health), Flask-SocketIO, cleaned up
- train_landmark_model.py: RF + MLP + TF.js export (belum bisa run — tensorflow gak support python 3.14)
- web/test.html: Browser TF.js inference page (BELUM WORK — model.json belum ada)
- web/capture.html: Phone capture → POST ke laptop (BUTUH laptop nyala)
- web/index.html: Landing page
- vercel.json: deployed ke Vercel
- start.sh, start-tunnel.sh, README.md
- Cleaned up: dead files, mediapipe assets, .gitignore updated
- Pushed ke GitHub + Vercel

YANG BELUM KESELESAIAN (TODO):
1. Train model → butuh python 3.11/3.12 (tensorflow gak support 3.14). 
   Cara: conda create -n bisindo python=3.11 → pip install tensorflow tensorflowjs → python train_landmark_model.py
   Output: web/models/model.json + labels.json + scaler.json (untuk test page)
2. Test page (/test.html) belum jalan karena model belum ada
3. Capture page (/capture.html) butuh laptop nyala (by design — data dikirim ke CSV laptop)
4. Cloudflare tunnel belum setup (optional)
5. Data collection: target 50,000 samples/letter (26 letters × 5 users) — baru ada 11,099 rows (campuran kaggle + legacy)

PENTING:
- dataset/ adalah git submodule (CSV gak dipush ke GitHub)
- Model training butuh venv python 3.11/3.12, bukan system python 3.14
- Test page harusnya 100% browser-only (TF.js), tapi model belum diexport
- Capture page emang butuh laptop (REST API → CSV)
- Remote: git@github.com:ItsKevinRafaell/bisindo-bridge.git
- Vercel: auto-deploy tiap push ke main

NEXT ACTION YANG GW MAU:
- [ISI DI SINI: mau lanjut apa? train model, fix test page, setup tunnel, dll]
```

---
